import React  from "react"

import { createSvgIcon } from '@mui/material/utils';

export const IconO = createSvgIcon(
    <circle
        fill="none"
        stroke="currentColor"
        strokeWidth={4}
        cx={12}
        cy={12}
        r={10}
    />,
    'IconO'
);
